# environment-manager

This skill is installed by AgentDock with:

```sh
bunx skills add <github-repository> --global --skill environment-manager --agent <configured-agents...> --yes --copy
```

Publish the repository to GitHub and set its URL in AgentDock Settings before release.
